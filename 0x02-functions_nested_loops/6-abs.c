#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: the int to check
 * Return the abolute value of int
 */

int _abs(int)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
