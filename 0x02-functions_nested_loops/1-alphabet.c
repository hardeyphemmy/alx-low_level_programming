#include "main.h"
/**
 * print_alphabet - print all alphabet in lower case
 */

void print_alphabet(void)
{
	char lette;

	for (letter = 'a'; leter <= 'z'; letter++)
		_putchar(letter);

	_putchar('\n');
}
